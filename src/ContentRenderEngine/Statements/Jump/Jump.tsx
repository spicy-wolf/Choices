import React from 'react';
import { AbstractStatementType } from '../AbstractComponentType.type';

export type JumpProps = { destination: string } & AbstractStatementType;

const Jump = (props: JumpProps): JSX.Element => {
  return <></>;
};

export default Jump;
