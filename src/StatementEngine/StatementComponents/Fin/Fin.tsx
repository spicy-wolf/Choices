import React from 'react';
import * as Types from '@src/Types';

const Fin = (props: Types.Statements.FinStatementType): JSX.Element => {
  return <div id={props.id} className="d-none"></div>;
};

export default Fin;
